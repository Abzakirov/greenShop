import Blogs from "../../../utils/Blogs";

const SectionTwo = () => {
  return (
    <div className="flex items-center gap-8 mt-[150px]">
      <Blogs />
    </div>
  );
};

export default SectionTwo;
